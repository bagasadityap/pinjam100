export interface BranchRequest {
  name: string;
  province: string;
  city: string;
  postalCode: string;
}

export interface BranchResponse {
  id: string;
  name: string;
  province: string;
  city: string;
  postalCode: string;
}
